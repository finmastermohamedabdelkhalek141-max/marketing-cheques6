# رفع التطبيق على GitHub — تعليمات سريعة

## الملف
- `index.html` — التطبيق الكامل (لازم يفضل اسمه `index.html` بالظبط عشان GitHub Pages يشتغل تلقائيًا كصفحة رئيسية).

## أول مرة (رفع المشروع)
1. روح على https://github.com واعمل مستودع جديد (Repository)، مثلاً باسم `marketing-cheques-app`.
   - اتركه **Public** أو **Private** حسب رغبتك (لو Private، GitHub Pages بيحتاج خطة مدفوعة أو تظل تستخدمه بدون Pages وتفتحه محليًا فقط).
   - متعملش تفعيل "Add a README" عشان متتعارضش مع الرفع.
2. اضغط **Add file → Upload files** وارفع ملف `index.html` (من نفس المجلد ده).
3. اكتب رسالة commit زي "رفع أولي للتطبيق" واضغط **Commit changes**.

## تفعيل الرابط المباشر (GitHub Pages) — اختياري لكن مفيد
1. في المستودع، روح **Settings → Pages**.
2. تحت "Build and deployment" اختار **Source: Deploy from a branch**.
3. اختار **Branch: main** و **Folder: / (root)** ثم **Save**.
4. بعد دقيقة أو اثنين هيظهر لك رابط زي:
   `https://<اسم-المستخدم>.github.io/marketing-cheques-app/`
   وده الرابط اللي تفتح بيه التطبيق من أي جهاز أو تليفون.

## تحديث الملف لاحقًا (بعد أي تعديل)
- من واجهة GitHub: افتح `index.html` في المستودع → اضغط أيقونة القلم (Edit) → الصق المحتوى الجديد كامل → Commit changes.
- أو ببساطة: **Add file → Upload files** وارفع نفس `index.html` تاني، هيسألك يستبدل القديم، وافق.

## ملاحظة أمان مهمة
- ملف `index.html` فيه إعدادات مشروع Firebase (وهي معلومات عامة تُستخدم من المتصفح وليست سرًا خطيرًا بحد ذاتها)، لكن الحماية الفعلية للبيانات موجودة في:
  - **Firebase Authentication** (الموظفين الخمسة فقط لهم حسابات).
  - **Firestore Security Rules** (`allow read, write: if request.auth != null;`) — يعني أي حد مش مسجل دخول بحساب من الحسابات دي، مش هيقدر يوصل للبيانات خالص حتى لو شاف الملف.
- محدش هيقدر يفتح البيانات أو يعدلها غير الموظفين اللي عندهم حساب فعلي في Firebase.
